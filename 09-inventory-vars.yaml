- name: inventory variables demo
  hosts: local
  connection: local
  tasks:
    - name: print course info
      ansible.builtin.debug:
        msg: "course is {{ COURSE }} and time {{ DURATION }} and trainee {{ TRAINER }}"
